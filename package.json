{
  "name": "news-writer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "apps/desktop/out/main/index.js",
  "engines": {
    "node": "24.18.0"
  },
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "tsc --build --pretty false && corepack pnpm --filter @news-writer/desktop build",
    "dev": "corepack pnpm --filter @news-writer/desktop dev",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "licenses:check": "node scripts/check-licenses.mjs",
    "public:check": "node scripts/check-public-release.mjs",
    "build:private-profile": "node scripts/build-private-profile.mjs",
    "package:private-profile": "corepack pnpm build && node scripts/package-private-profile.mjs",
    "private:profile:build": "pnpm --filter @news-writer/retrieval build && pnpm --filter @news-writer/institution build && node scripts/build-private-profile.mjs",
    "lint": "eslint .",
    "package:dir": "corepack pnpm licenses:check && corepack pnpm build && node scripts/package-dir.mjs",
    "package:smoke": "corepack pnpm --filter @news-writer/desktop build:package-smoke && node scripts/package-smoke-dir.mjs",
    "test": "corepack pnpm test:unit && corepack pnpm test:component",
    "test:component": "vitest run --project component",
    "test:e2e": "corepack pnpm build && corepack pnpm --filter @news-writer/desktop build:e2e && playwright test --config playwright.config.ts",
    "test:package": "corepack pnpm package:smoke && playwright test --config playwright.package.config.ts",
    "test:unit": "corepack pnpm --filter @news-writer/project build && corepack pnpm --filter @news-writer/ai build && corepack pnpm --filter @news-writer/documents build && vitest run --project unit",
    "typecheck": "tsc --noEmit -p tsconfig.json && corepack pnpm -r --if-present typecheck",
    "verify": "corepack pnpm public:check && corepack pnpm format:check && corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test && corepack pnpm build"
  },
  "devDependencies": {
    "@electron/asar": "3.4.1",
    "@electron/fuses": "1.8.0",
    "@playwright/test": "1.62.1",
    "@testing-library/react": "16.3.2",
    "@testing-library/user-event": "14.6.3",
    "@types/node": "24.13.3",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitejs/plugin-react": "5.2.0",
    "@vitest/coverage-v8": "4.1.10",
    "electron": "43.3.0",
    "electron-builder": "26.15.3",
    "electron-vite": "5.0.0",
    "eslint": "10.8.1",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.5.3",
    "happy-dom": "20.11.2",
    "mammoth": "1.12.0",
    "pdfjs-dist": "6.2.108",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.66.0",
    "vite": "7.3.6",
    "vitest": "4.1.10"
  }
}
