# 面向低 AI 基础人群的图形化 AI 新闻稿撰写辅助系统

## 1. 目标

本项目在 `news_writer` 中实现一个面向 Windows 10/11 x64 的桌面新闻稿写作工作台，帮助 AI 和计算机基础较弱的用户完成从活动纪要到最终 Word 新闻稿的完整流程。

原项目 `news` 只作为功能规格、业务规则、Prompt、写作规范、输入输出样例和行为验收基准。不得修改原项目，也不得复用、迁移或调用其中的 Python 和 PowerShell 实现。

## 2. 技术架构

- Electron + React + TypeScript + Monaco Editor。
- 使用纯 TypeScript 重新设计和实现业务能力，不使用 Python sidecar。
- Electron 自带 Chromium，不以安装体积或轻量化为首要目标。
- 耗时或计算密集型任务使用 Worker Threads 或 Electron `utilityProcess`，不得阻塞 renderer 或主进程。
- renderer 不直接访问 Node.js、文件系统或凭据，只能调用受控 preload API。
- 所有 IPC 请求和响应必须经过运行时 Schema 校验。
- 优先使用纯 JavaScript 或 WASM 依赖，避免不必要的原生 Node 扩展和 Electron ABI 重编译。

建议工程结构：

```text
news_writer/
  apps/
    desktop/
      main/
      preload/
      renderer/
  packages/
    domain/
    project/
    ai/
    retrieval/
    documents/
    shared/
  resources/
    institution/
      knowledge/
  tests/
    fixtures/
    golden/
```

## 3. 首版功能范围

首版包含普通用户的完整写稿流程：

1. 新建、打开和归档项目。
2. 支持学院新闻稿、其他新闻稿两种类型。
3. 创建、导入和编辑活动纪要。
4. 检索内置历史参考稿并保存检索记录。
5. 生成、预览和编辑 Prompt。
6. 生成初稿，检测并补充缺失信息。
7. AI 二次审稿。
8. 对当前最新版进行人工批注，并根据批注续改。
9. 浏览历史版本、比较 diff、切换任意历史版本为最新版。
10. 从历史版本继续生成并形成版本分支。
11. 将指定版本导出为最终 DOCX。

首版明确不包含：

- 知识库管理及任何面向用户的知识库入口。
- 历史资料扫描、压缩包整理、去重和知识库重建工具。
- 将项目稿件自动加入知识库。
- 原项目 `outputs` 兼容或旧项目导入。
- 流式生成、多 AI 供应商公开配置、自动更新、安装包和正式代码签名流程。

项目归档只改变项目状态，不改变版本内容，也不会把稿件加入知识库。

## 4. 内置知识库

机构 profile 随包携带只读知识库：

```text
resources/institution/knowledge/
  corpus.jsonl
  index.json
  training_rules.txt
  metadata.json
```

- `corpus.jsonl` 只包含清理、脱敏后的规范化文本。
- `index.json` 是预构建检索索引。
- `training_rules.txt` 保存新闻稿培训规则。
- `metadata.json` 保存来源范围、构建时间、内容哈希、算法和知识库版本。
- 不携带原始 DOC、DOCX、PDF、PPTX、图片等文件。
- 首版知识库只读，用户不能增加、删除或重建。
- 首版暂不处理不同 App 知识库版本之间的项目兼容问题。

## 5. 界面

- 保留工作台性质，不做成一键生成工具。
- 顶部提供文件、编辑、项目等常规菜单。
- 资源树默认折叠。
- 批注区默认开启，并与当前最新版联动。
- 中间使用 Monaco 双栏：左侧用于纪要、Prompt、历史稿等内容；右侧默认显示最新版，并支持历史版本 diff。
- UI 必须明确显示当前最新版、当前有效版本链、分支来源和历史分支。
- 高级配置可以折叠，但不能移除用户对生成过程的控制。
- 按钮、专有名词和重要状态提供适当的悬浮解释、引导和错误说明。
- 任务只显示当前状态和已发生的阶段，不显示虚假的百分比进度。

## 6. 版本模型

版本不能只依赖“第 N 版”名称或文件名。每个成功版本至少包含：

- `id`：不可变唯一标识。
- `createdAt`：高精度时间戳。
- `parentVersionId`：生成该版本所基于的父版本。
- 实际发送 Prompt 的引用。
- 生成配置快照。
- 内容文件引用。
- 产生该版本的成功任务引用和状态快照。

项目以 `latestVersionId` 表示当前最新版：

1. 成功版本不可变，不直接覆盖或删除。
2. 用户可以把任意历史版本设为最新版。
3. 切换最新版不删除其他版本；不在当前父子链上的版本保留为历史分支。
4. 用户可以重新切回原最新版，恢复原版本链。
5. 从历史版本继续生成时形成新分支。
6. `parentVersionId` 判断版本关系，`createdAt` 用于排序和生成人类可读名称。
7. 失败、取消、超时或空结果不创建版本，只创建任务记录。

## 7. 批注模型

批注是对应版本新闻稿的一部分，但不是导出文档内容：

- 批注永久跟随其 `versionId`，不会移动到新版本，也不会因切换最新版而丢失。
- 只有 `latestVersionId` 指向的版本允许新增或编辑批注。
- 把历史版本设为最新版后，可以查看并修改该版本当时的批注。
- 批注至少保存 `id`、`versionId`、稳定文本锚点、引用文本、正文、创建和更新时间。
- 从某版本生成新版本时，该版本当时的全部批注快照进入实际发送的修订 Prompt。
- 生成成功后，原版本及其批注继续保留；新版本不会自动继承这些批注。
- 从同一版本再次生成时，使用该版本届时保存的批注形成另一条分支。
- 导出任意版本为 DOCX 时忽略全部批注。

## 8. Prompt 与任务

- Prompt 可以编辑，不阻止用户删除或修改系统生成内容。
- 用户首次编辑生成的 Prompt 前，必须明确警告修改可能破坏事实约束和写作规范，结果由用户承担。
- 不额外保存系统原始 Prompt，只保存实际发送给 AI 的最终 Prompt 及其任务和版本关系。
- 首版只正式支持 DeepSeek；内部保留 OpenAI-compatible 客户端边界，但不承诺其他供应商兼容。
- 首版不支持流式生成。
- 任务必须支持明确的当前状态、取消和超时。
- 取消只表示客户端停止等待，不得承诺服务端一定停止处理或不会计费。
- 实际 Prompt、模型和推理强度必须与任务记录关联。
- 任务失败、取消或超时不得覆盖已有版本，也不得创建成功的空版本。

## 9. 配置与认证

配置覆盖顺序为：单次任务 > 项目配置 > 用户配置 > 默认值。

- 项目配置可保存新闻稿类型、模型、推理强度、目标渠道等非敏感设置。
- API Key 不存在项目级配置。
- API Key 和认证状态只保存在用户目录，隔离原则参考 Codex 的 `auth.json` 机制。
- 凭据优先使用 Electron `safeStorage` 和 Windows DPAPI 保护。
- 项目目录、导出文件、日志和 Prompt 中不得写入 API Key。
- 项目复制到其他电脑后不携带认证信息，接收者必须配置自己的 API Key。

## 10. 项目格式与可移植性

复制项目目录后必须保留活动纪要、非敏感配置、全部版本及关系、批注、实际 Prompt、任务和检索记录、导出记录。

项目目录不得包含 API Key、App 内置知识库或 Electron 运行环境。App 必须能从任意可写目录打开项目。

项目内容和版本数据优先采用追加写入；索引和可变元数据采用临时文件、落盘同步、原子替换、重试和恢复记录，避免崩溃造成半写入。

## 11. DOCX 导出

- 建立正式 DOCX 样板，明确标题、正文、落款、日期、字体、字号、缩进、对齐和段落间距。
- 中间预览和版本内容以应用内文本或 Markdown 为主。
- 只有用户明确导出时才生成 DOCX。
- 导出文件不得包含批注、Prompt、检索信息、问题清单或内部任务数据。
- DOCX 必须经过实际渲染和视觉验收，不能只验证文件可打开。

## 12. 平台、交付与验证

- 首版只支持 Windows 10/11 x64。
- 验收产物为完整便携目录。
- 用户不需要额外安装 Node.js、Python、conda 或浏览器。
- 安装包、自动更新和代码签名不属于首版验收；签名方案在交付阶段另行决定。
- 记录应用、Electron、Chromium、项目 Schema 和知识库版本，支持人工升级和问题排查。
- 验收包括完整操作流程、异常流程、项目复制、崩溃恢复、安全边界、实际 DOCX 渲染，以及无开发环境 Windows 测试。

## 13. 协作和实施约束

- 所有写入只能发生在 `news_writer`，不得修改 `news`。
- 任何新模块进入编码前，必须由独立 agent 检查必要性、已有复用能力和成熟开源方案，再交给 coding agent。
- 模块完成后应由独立 review agent 检查行为、安全、测试和重复实现。
- 主 agent 负责架构边界、任务分配、集成、验收和向用户汇报，不承担具体模块编码。
- 后续实施顺序和阶段退出条件见 `IMPLEMENTATION_PLAN.md`。
- agent 的通用工作规则见 `AGENTS.md`。
